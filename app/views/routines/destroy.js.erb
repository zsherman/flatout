next = $("li[data-id='<%= @routine.id %>']").next('li');
$("li[data-id='<%= @routine.id %>']").remove();
$('#exercise-list').empty();
$('li.routine a').first().trigger('click'); // not triggering because of delegate on list items - need to fix
console.log(next);
